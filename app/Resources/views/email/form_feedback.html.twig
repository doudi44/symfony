<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>mail</title>
    <!-- Tell the browser to be responsive to screen width -->
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
</head>
<body>
<h1>Vous avez reçu un message de <u>{{ data.firstname }} {{ data.lastname }}</u></h1>
<h5>({{ data.email }})</h5>

<h2>Problème de type : {{ data.bug }}</h2>

<h4>Sur la page : <a href="{{ data.page }}">{{ data.page }}</a></h4>

<h3>le {{ data.date.date|date("d/m/y") }}</h3>


<em>{{ data.message }}</em>

{% if copie != false %}
    <h3>{{ copie }}</h3>
{% endif %}


</body>
</html>